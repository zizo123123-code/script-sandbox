# 🧠 MEMORY.md — ذاكرة Arena.ai (AR) طويلة المدى
## دروس مستفادة + قرارات + معرفة تراكمية (Append-only)

> لا تُكتب أسرار أو توكنات أو cookies هنا. خزّن القرارات العامة فقط.

---

| التاريخ | الدرس / المعرفة |
|---|---|
| 2026-08-26 | `arena` هو اسم غرفة Arena.ai؛ اختصار البروتوكول المستخدم هو `AR` لأن الأكواد محدودة إلى 2–4 أحرف. |
| 2026-08-26 | لا يجوز اعتبار Arena.ai Provider حقيقياً بلا API/auth/capability evidence؛ أُنشئ كـ `template_disabled` و`is_functional: false`. |
| 2026-08-26 | إبقاء التنفيذ غير شبكي يحمي المستخدم من تكامل مُختلق أو تسريب اعتماد، ويجعل الاختبارات اختبارات عقد فقط. |
| 2026-08-26 | عند إصلاح NoteGPT، SPEC ظلّ مرجع المعمارية، بينما 01.06 ظلّ مرجع ترتيب الطلبات والـSSE؛ الإصلاح حافظ على `conversation_id` وغيّر فقط الانحرافات المثبتة في audit. |
| 2026-08-26 | `continue_needed` marker لا يبرر قطع HTTP response؛ يجب تصريف الاستجابة كلها، مع إبقاء حد auto-continue منفصلاً عن polling الإقلاع. |
